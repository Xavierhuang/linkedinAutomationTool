"""
User Preferences Routes - For managing user-specific preferences
This module is currently unused but reserved for future user preference functionality
"""

from fastapi import APIRouter

router = APIRouter(prefix="/user-preferences", tags=["user-preferences"])

# Placeholder for future user preference endpoints
# Example: Notification settings, UI preferences, content preferences, etc.
